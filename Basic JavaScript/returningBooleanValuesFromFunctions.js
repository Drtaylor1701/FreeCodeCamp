//all comparison operators return True or False

//sometimes pepole use if/else to do the comparison
function isEqual(a,b) {
    if (a===b) {
        return true;
    } else {
        return false;
    }
}

//or you could just do this:
function isEqual(a,b) {
    return a===b;
}