#! JavaScript

//case values are tested with strict equality (===)

switch(lowercaseLetter) {
    case "a":
        console.log("A");
        ///tells JavaScript to stop executing statements
        /// if the break statement is omitted, the next statement will be executed
        break;
    case "b":
        console.log("B");
        break;
}

    